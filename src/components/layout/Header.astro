---
import { getCollection } from 'astro:content';
import { Search, Heart, GitCompare, Menu, X } from 'lucide-react';

const categories = await getCollection('categories').catch(() => []);
const topLevel = categories
  .filter((c) => !c.data.parent)
  .sort((a, b) => a.data.order - b.data.order)
  .slice(0, 6);
---

<header class="site-header">
  <div class="container-page site-header__inner">
    <a href="/" class="site-header__logo" aria-label="HomeSpaGuide home">
      <span class="logo-mark" aria-hidden="true">◐</span>
      <span class="logo-text">HomeSpaGuide</span>
    </a>

    <nav class="site-header__nav" aria-label="Primary">
      <ul>
        {topLevel.map((cat) => (
          <li>
            <a href={`/category/${cat.slug}`}>{cat.data.name}</a>
          </li>
        ))}
        <li><a href="/guides">Guides</a></li>
        <li><a href="/blog">Blog</a></li>
      </ul>
    </nav>

    <div class="site-header__actions">
      <button class="icon-btn" data-search-trigger aria-label="Search">
        <Search size={20} aria-hidden="true" />
      </button>
      <a href="/wishlist" class="icon-btn icon-btn--badge" aria-label="Wishlist">
        <Heart size={20} aria-hidden="true" />
        <span data-wishlist-count class="badge" hidden>0</span>
      </a>
      <a href="/compare" class="icon-btn icon-btn--badge" aria-label="Compare">
        <GitCompare size={20} aria-hidden="true" />
        <span data-compare-count class="badge" hidden>0</span>
      </a>
      <button class="icon-btn site-header__menu-toggle" data-mobile-toggle aria-label="Menu" aria-expanded="false">
        <Menu size={22} aria-hidden="true" />
      </button>
    </div>
  </div>

  <div class="mobile-drawer" data-mobile-drawer hidden>
    <div class="mobile-drawer__head">
      <span class="logo-text">HomeSpaGuide</span>
      <button class="icon-btn" data-mobile-close aria-label="Close menu">
        <X size={22} aria-hidden="true" />
      </button>
    </div>
    <ul class="mobile-drawer__list">
      {topLevel.map((cat) => (
        <li><a href={`/category/${cat.slug}`}>{cat.data.name}</a></li>
      ))}
      <li><a href="/guides">Buying Guides</a></li>
      <li><a href="/blog">Blog</a></li>
      <li><a href="/about">About</a></li>
      <li><a href="/wishlist">Wishlist</a></li>
      <li><a href="/compare">Compare</a></li>
    </ul>
  </div>
</header>

<style>
  .site-header {
    position: sticky;
    top: 0;
    z-index: 40;
    background: rgba(255, 255, 255, 0.92);
    backdrop-filter: saturate(150%) blur(10px);
    border-bottom: 1px solid var(--color-border);
  }
  .site-header__inner {
    display: flex;
    align-items: center;
    gap: 1.25rem;
    height: var(--header-height);
  }
  .site-header__logo {
    display: inline-flex;
    align-items: center;
    gap: 0.55rem;
    color: var(--color-primary);
    font-family: var(--font-serif);
    font-weight: 700;
    font-size: 1.25rem;
    letter-spacing: -0.01em;
  }
  .logo-mark { color: var(--color-accent); font-size: 1.5rem; line-height: 1; }
  .site-header__nav {
    flex: 1;
  }
  .site-header__nav ul {
    display: none;
    list-style: none;
    padding: 0;
    margin: 0;
    gap: 1.25rem;
  }
  .site-header__nav a {
    color: var(--color-ink-muted);
    font-size: var(--text-sm);
    font-weight: 500;
    padding: 0.5rem 0;
    border-bottom: 2px solid transparent;
    transition: color 0.15s ease, border-color 0.15s ease;
  }
  .site-header__nav a:hover {
    color: var(--color-primary);
    border-bottom-color: var(--color-accent);
  }
  .site-header__actions {
    display: flex;
    align-items: center;
    gap: 0.5rem;
  }
  .icon-btn {
    position: relative;
    display: inline-flex;
    align-items: center;
    justify-content: center;
    width: 40px;
    height: 40px;
    border-radius: var(--radius-md);
    color: var(--color-ink-muted);
    transition: background 0.15s ease, color 0.15s ease;
  }
  .icon-btn:hover { background: var(--color-surface-muted); color: var(--color-primary); }
  .badge {
    position: absolute;
    top: 4px;
    right: 4px;
    min-width: 18px;
    height: 18px;
    padding: 0 4px;
    background: var(--color-accent);
    color: var(--color-primary-900);
    border-radius: var(--radius-full);
    font-size: 11px;
    font-weight: 700;
    display: inline-flex;
    align-items: center;
    justify-content: center;
  }
  .site-header__menu-toggle { display: inline-flex; }
  .mobile-drawer {
    position: fixed;
    inset: 0;
    background: var(--color-surface);
    z-index: 50;
    padding: 1.25rem;
    overflow-y: auto;
  }
  .mobile-drawer__head {
    display: flex;
    justify-content: space-between;
    align-items: center;
    margin-bottom: 1.5rem;
  }
  .mobile-drawer__list {
    list-style: none;
    padding: 0;
    margin: 0;
    display: flex;
    flex-direction: column;
    gap: 0.25rem;
  }
  .mobile-drawer__list a {
    display: block;
    padding: 0.85rem 0.5rem;
    color: var(--color-ink);
    font-size: var(--text-lg);
    border-bottom: 1px solid var(--color-border);
  }

  @media (min-width: 900px) {
    .site-header__nav ul { display: flex; }
    .site-header__menu-toggle { display: none; }
  }
</style>

<script>
  const toggle = document.querySelector<HTMLButtonElement>('[data-mobile-toggle]');
  const close = document.querySelector<HTMLButtonElement>('[data-mobile-close]');
  const drawer = document.querySelector<HTMLDivElement>('[data-mobile-drawer]');
  toggle?.addEventListener('click', () => {
    drawer?.removeAttribute('hidden');
    toggle.setAttribute('aria-expanded', 'true');
  });
  close?.addEventListener('click', () => {
    drawer?.setAttribute('hidden', '');
    toggle?.setAttribute('aria-expanded', 'false');
  });

  function readCount(key: string): number {
    try {
      const raw = localStorage.getItem(key);
      if (!raw) return 0;
      const arr = JSON.parse(raw);
      return Array.isArray(arr) ? arr.length : 0;
    } catch {
      return 0;
    }
  }
  function update(selector: string, count: number) {
    const el = document.querySelector<HTMLElement>(selector);
    if (!el) return;
    if (count > 0) {
      el.hidden = false;
      el.textContent = String(count);
    } else {
      el.hidden = true;
    }
  }
  update('[data-wishlist-count]', readCount('hsg.wishlist'));
  update('[data-compare-count]', readCount('hsg.compare'));
  window.addEventListener('storage', () => {
    update('[data-wishlist-count]', readCount('hsg.wishlist'));
    update('[data-compare-count]', readCount('hsg.compare'));
  });
  window.addEventListener('hsg:wishlist:change', () => update('[data-wishlist-count]', readCount('hsg.wishlist')));
  window.addEventListener('hsg:compare:change', () => update('[data-compare-count]', readCount('hsg.compare')));

  const search = document.querySelector<HTMLButtonElement>('[data-search-trigger]');
  search?.addEventListener('click', () => {
    window.dispatchEvent(new CustomEvent('hsg:search:open'));
  });
</script>
