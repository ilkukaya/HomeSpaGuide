---
import '../styles/globals.css';
import SEO from '../components/seo/SEO.astro';
import Header from '../components/layout/Header.astro';
import Footer from '../components/layout/Footer.astro';
import SearchOverlay from '../components/layout/SearchOverlay.astro';

interface Props {
  title: string;
  description: string;
  canonical?: string;
  ogImage?: string;
  ogType?: 'website' | 'article' | 'product';
  noindex?: boolean;
  publishedTime?: string;
  modifiedTime?: string;
}

const props = Astro.props;
const plausibleDomain = import.meta.env.PUBLIC_PLAUSIBLE_DOMAIN;
const plausibleScript = import.meta.env.PUBLIC_PLAUSIBLE_SCRIPT;
const siteUrl = import.meta.env.PUBLIC_SITE_URL || 'https://homespaguide.com';

const orgJsonLd = {
  '@context': 'https://schema.org',
  '@type': 'Organization',
  name: 'HomeSpaGuide',
  url: siteUrl,
  logo: new URL('/favicon.svg', siteUrl).toString(),
  sameAs: [],
};
---

<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <link rel="sitemap" href="/sitemap-index.xml" />
    <link rel="alternate" type="application/rss+xml" title="HomeSpaGuide Blog" href="/rss.xml" />
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link
      rel="stylesheet"
      href="https://fonts.googleapis.com/css2?family=DM+Serif+Display:ital@0;1&family=DM+Sans:opsz,wght@9..40,400;9..40,500;9..40,600;9..40,700&display=swap"
    />
    <SEO {...props} />
    <script type="application/ld+json" set:html={JSON.stringify(orgJsonLd)} />
    {plausibleDomain && plausibleScript && (
      <script
        is:inline
        defer
        data-domain={plausibleDomain}
        src={plausibleScript}
      />
    )}
    <slot name="head" />
  </head>
  <body>
    <a href="#main" class="skip-link">Skip to content</a>
    <Header />
    <main id="main">
      <slot />
    </main>
    <Footer />
    <SearchOverlay />
    <slot name="end" />
  </body>
</html>
